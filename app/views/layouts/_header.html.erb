<header>
  <p id="logo"><%= link_to "つべログ", root_url%></p>
  <nav>
      <ul>
          <li><%= link_to "トップ", root_url %></li>
          <% if !user_signed_in? %>
          <li><%= link_to "新規登録", new_user_registration_path %></li>
          <% else %>
          <li><%= link_to "新規投稿", new_review_path %></li>
          <% end %>
          <% if !user_signed_in? %>
            <li><%= link_to "ログイン", new_user_session_path %></li>
          <% else %>
            <li><%= link_to "ログアウト", destroy_user_session_path, :method => :delete %></li>
          <% end %>
          <li><%= link_to "検索", search_path %></li>
          <div class="clear"></div>
      </ul>
  </nav>
    <div class="clear"></div>
</header>
